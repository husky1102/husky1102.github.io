services:
  site:
    build: .
    ports: ["4000:4000"]
    volumes:
      - .:/workspace
      - site-node-modules:/workspace/node_modules
    command: sh -c "npm ci && npm run build:js && npm run build:font && bundle exec jekyll serve --host 0.0.0.0"
volumes:
  site-node-modules:
